LevelBuilder.name = Level

FlattenBuilder.name = Flatten
FlattenBuilder.description = Keep only the current view in the hierarchy and transform meta edges in normal edges. The graph is no more hierarchic.